<<< spec/sync_spec.rb
>>> rubyspec/core/io/sync_spec.rb
1c1
< describe "IO::Like#sync=" do
---
> describe "IO#sync=" do
6,7c6,7
<   it "raises IOError on closed stream" do
< describe "IO::Like#sync" do
---
>   it "raises an IOError on closed stream" do
> describe "IO#sync" do
11c11
<   it "raises IOError on closed stream" do
---
>   it "raises an IOError on closed stream" do
#lwoggardner: all tests covered, spec deleted
