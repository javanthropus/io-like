<<< spec/lines_spec.rb
>>> rubyspec/core/io/lines_spec.rb
3,4c3,4
<     before(:each) do
<     after(:each) do
---
>     before :each do
>     after :each do
6,7c6,10
#lwoggardner: covered below
<     it "ignores a given block" do
<       @iowrapper.lines do
---
>     it "returns a line when accessed" do
>     ruby_version_is ''...'1.9' do
>       it "ignores a given block" do
>     ruby_version_is '1.9' do
>       it "yields each line to the passed block" do
#lwoggardnr: all tests covered, spec deleted
