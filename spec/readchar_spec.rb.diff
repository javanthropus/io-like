<<< spec/readchar_spec.rb
>>> rubyspec/core/io/readchar_spec.rb
1c1
< describe "IO::Like#readchar" do
---
> describe "IO#readchar" do
4,7c4,8
#lwoggardner: covered under "... 1.9" below
<   it "returns the next byte from the stream" do
#lwoggardner: covered under "... 1.9" below
<   it "raises EOFError when invoked at the end of the stream" do
#lwoggardner: not technically covered, but unsure of value in comparison
# to test above, particularly as the only reason for having this spec
<   it "raises EOFError when reaches the end of the stream" do
#lwoggardner: covered below
<   it "raises EOFError on empty stream" do
#lwoggardner:  all (valued) tests covered, spec deleted
---
>   ruby_version_is "" ... "1.9" do
>     it "returns the next byte from the stream" do
>   ruby_version_is "1.9" do
>     it "returns the next string from the stream" do
>   it "raises an EOFError when invoked at the end of the stream" do
8a10,13
> describe "IO#readchar" do
>   before :each do
>   after :each do
>   it "raises EOFError on empty stream" do
