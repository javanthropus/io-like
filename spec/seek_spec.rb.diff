<<< spec/seek_spec.rb
>>> rubyspec/core/io/seek_spec.rb
1c1,3
< describe "IO::Like#seek" do
---
> describe "IO#seek" do
>   it_behaves_like :io_set_pos, :seek
> describe "IO#seek" do
7,8d8
#lwoggardner: covered by various tests on rubyspec/core/io/shared/pos.rb
<   it "can handle any numerical argument without breaking" do
#lwoggardner: covered by rubyspec/core/io/shared/pos.rb
<   it "raises IOError on closed stream" do
#lwoggardner: all tests covered, spec deleted
