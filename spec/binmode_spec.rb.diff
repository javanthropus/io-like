<<< spec/binmode_spec.rb
>>> rubyspec/core/io/binmode_spec.rb
1c1
< describe "IO::Like#binmode" do
---
> describe "IO#binmode" do
4,7c4,15
<   it "returns a reference to the stream" do
<   ruby_version_is ''...'1.8.7' do
<     it "does not raise any errors on closed stream" do
<   it "puts a stream in binary mode" do
#lwoggardner: covered by "returns self"
<   it "returns a reference to the stream" do
#lwoggardner: covered (not attempting to match ruby bug levels)
<   ruby_version_is ''...'1.8.7' do
<     it "does not raise any errors on closed stream" do
#lwoggardner: already redundantly covered by "returns a reference to the stream"
<   it "puts a stream in binary mode" do
#all tests covered, spec deleted
---
>   it "returns self" do
>   ruby_bug "#2046", "1.8.7.174" do
>     it "raises an IOError on closed stream" do
>   ruby_version_is "1.9" do
>     it "sets external encoding to binary" do
>     it "sets internal encoding to nil" do
> ruby_version_is "1.9" do
>   describe "IO#binmode?" do
>     before :each do
>     after :each do
>     it "is true after a call to IO#binmode" do
>     it "propagates to dup'ed IO objects" do
