<<< spec/each_spec.rb
>>> rubyspec/core/io/each_spec.rb
#lwoggardner: see shared/each.rb.diff
1,4c1,4
< describe "IO::Like#each" do
<   it_behaves_like :io_like__each, :each
< describe "IO::Like#each when passed a separator" do
<   it_behaves_like :io_like__each_separator, :each
---
> describe "IO#each" do
>   it_behaves_like :io_each, :each
> describe "IO#each" do
>   it_behaves_like :io_each_default_separator, :each
