<<< spec/printf_spec.rb
>>> rubyspec/core/io/printf_spec.rb
1c1
< describe "IO::Like#printf" do
---
> describe "IO#printf" do
4,5c4,5
#lwoggardner: covered below
<   it "writes the #sprintf formatted string to the file descriptor" do
<     lambda do
---
>   it "calls #to_str to convert the format object to a String" do
>   it "writes the #sprintf formatted string" do
#lwoggardner: all tests covered, spec deleted
