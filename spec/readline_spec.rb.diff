<<< spec/readline_spec.rb
>>> rubyspec/core/io/readline_spec.rb
1c1
< describe "IO::Like#readline" do
---
> describe "IO#readline" do
6,7c6
#lwoggardner: covered by "returns characters after #seek"
<   it "is modified by the cursor position" do
#lwoggardner: retained
<   it "reads and returns all data available before a SystemCallError is raised when the separator is nil" do
---
>   it "returns characters after the position set by #seek" do
9a9
>   it "assigns the returned line to $_" do
