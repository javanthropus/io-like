<<< spec/ungetc_spec.rb
>>> rubyspec/core/io/ungetc_spec.rb
1c1,2
< describe "IO::Like#ungetc" do
---
> ruby_version_is "1.9" do
> describe "IO#ungetc" do
12c13,16
#lwoggardner: covered below (up to 1.9)
<   #it "raises IOError when invoked on stream that was not yet read" do
---
>   ruby_version_is "" ... "1.9" do
>     it "raises IOError when invoked on stream that was not yet read" do
>   ruby_version_is "1.9" do
>     it "returns nil when invoked on stream that was not yet read" do
#lwoggardner: all tests covered, spec deleted
